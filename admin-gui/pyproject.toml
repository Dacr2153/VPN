[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "vpnforge-admin"
version = "0.1.0"
description = "VPNForge admin panel — PySide6 GUI for server management"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "VPNForge" }]

dependencies = [
    "PySide6>=6.6.0",
    "grpcio>=1.60.0",
    "grpcio-tools>=1.60.0",
    "protobuf>=4.25.0",
    "PySide6-Charts>=6.6.0",
    "networkx>=3.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-qt>=4.2",
    "mypy>=1.8",
]

[project.scripts]
vpnforge-admin = "vpnforge_admin:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["vpnforge_admin*"]
